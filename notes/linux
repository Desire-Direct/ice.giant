# FEDORA
sudo -i 			#switch to root
sudo dnf upgrade		#update
sudo dnf install package-name	#install package
dnf list installed package-name	#package installed?
# user permissions
ls -l	#display permissions
sudo chown -R username:usergroup directory	#change permission for dir and contents

# GIT 
git config --list	#check configuration settings (.gitconfig)

# mount encrypted partition
sudo cryptsetup --type luks open /dev/sdX crypt
sudo mount -t ext4 /dev/mapper/crypt /mnt/myusb
# unmount encrypted partition
sudo umount /mnt/myusb
sudo cryptsetup close crypt

# rsync
# -P stands for show progress
sudo rsync -r -P source destination

# LaTeX
installed texlive-scheme-basic, 'tex(beamer.cls)', 'tex(hyperref.sty)', texlive-biblatex, biber.noarch 2.21-4.fc44;
#compile with bibliography 
pdflatex document.tex
biber document
pdflatex document.tex

# VIM
:echo has('clipboard')	#clipboard enabled? 0
(could swear it worked in arch)
gvim and vim-X11 installed
Solution: open files in split window?
:split ~/path to file (vsplit vertical)
move about: Ctrl+w followed by (h, j, k, l);

# SOUND 
with pulseaudio/pipewire for HDMI output;
pactl list cards
#this is not temporary!?:
pactl set-card-profile alsa_card.pci-0000_00_1f.3 output:hdmi-stereo

# WEB SERVER
config /load modules in /etc/httpd/conf.d;
opened firewall;


